apiVersion: v1
kind: Service
metadata:
  name: docker-registry-service
  namespace: infra
spec:
  selector:
    app: docker-registry
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort
