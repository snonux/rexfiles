if type -q bat
    alias Cat=/usr/bin/cat
    alias cat=bat
end
if type -q see
    alias ca=see
end
if type -q bit
    alias Git=/usr/bin/git
    alias git=bit
end
if type -q procs
    alias p='procs'
end
